#pragma once
#include <cstdint>
#include <string>

namespace fb::api
{
    using mheight       = uint16_t;
    using mweight       = uint16_t;
    using rating        = uint16_t;
    using object_id     = std::string;
}